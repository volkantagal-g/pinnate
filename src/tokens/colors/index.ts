import { defaultTokens } from '../default';

export const palette = defaultTokens.palette;
export const paletteColors = defaultTokens.palette;

export type { PaletteTokens, PaletteShade, PaletteScale } from '../types';
