import { defaultTokens } from '../default';

export const badge = defaultTokens.badge;

export const { gray, purple, red, orange, yellow, green, teal, blue, pink } = badge;

export type { BadgeTokens } from '../types';
